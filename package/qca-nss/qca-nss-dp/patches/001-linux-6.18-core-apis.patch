diff --git a/include/nss_dp_dev.h b/include/nss_dp_dev.h
index f8d0b84..5d0baf5 100644
--- a/include/nss_dp_dev.h
+++ b/include/nss_dp_dev.h
@@ -229,11 +229,10 @@ struct nss_dp_dev {
 	unsigned long drv_flags;	/* Driver specific feature flags */
 
 	/* Phy related stuff */
+	struct device_node *phy_node;	/* Phy device OF node */
 	struct phy_device *phydev;	/* Phy device */
 	struct mii_bus *miibus;		/* MII bus */
 	uint32_t phy_mii_type;		/* RGMII/SGMII/QSGMII */
-	uint32_t phy_mdio_addr;		/* Mdio address */
-	bool link_poll;			/* Link polling enable? */
 	uint32_t forced_speed;		/* Forced speed? */
 	uint32_t forced_duplex;		/* Forced duplex? */
 	uint32_t link_state;		/* Current link state */
diff --git a/nss_dp_ethtools.c b/nss_dp_ethtools.c
index ddfe0c1..a9bd3cc 100644
--- a/nss_dp_ethtools.c
+++ b/nss_dp_ethtools.c
@@ -248,12 +248,15 @@ static inline void nss_dp_fal_to_ethtool_linkmode_xlate(uint32_t *xlate_to, uint
  * nss_dp_get_eee()
  *	Get EEE settings.
  */
-static int32_t nss_dp_get_eee(struct net_device *netdev, struct ethtool_eee *eee)
+static int32_t nss_dp_get_eee(struct net_device *netdev, struct ethtool_keee *eee)
 {
 	struct nss_dp_dev *dp_priv = (struct nss_dp_dev *)netdev_priv(netdev);
 	fal_port_eee_cfg_t port_eee_cfg;
 	uint32_t port_id;
 	sw_error_t ret;
+	u32 supported = 0;
+	u32 advertised = 0;
+	u32 lp_advertised = 0;
 
 	memset(&port_eee_cfg, 0, sizeof(fal_port_eee_cfg_t));
 	port_id = dp_priv->macid;
@@ -266,9 +269,12 @@ static int32_t nss_dp_get_eee(struct net_device *netdev, struct ethtool_eee *eee
 	/*
 	 * Translate the FAL linkmode types to ethtool linkmode types.
 	 */
-	nss_dp_fal_to_ethtool_linkmode_xlate(&eee->supported, &port_eee_cfg.capability);
-	nss_dp_fal_to_ethtool_linkmode_xlate(&eee->advertised, &port_eee_cfg.advertisement);
-	nss_dp_fal_to_ethtool_linkmode_xlate(&eee->lp_advertised, &port_eee_cfg.link_partner_advertisement);
+	nss_dp_fal_to_ethtool_linkmode_xlate(&supported, &port_eee_cfg.capability);
+	nss_dp_fal_to_ethtool_linkmode_xlate(&advertised, &port_eee_cfg.advertisement);
+	nss_dp_fal_to_ethtool_linkmode_xlate(&lp_advertised, &port_eee_cfg.link_partner_advertisement);
+	ethtool_convert_legacy_u32_to_link_mode(eee->supported, supported);
+	ethtool_convert_legacy_u32_to_link_mode(eee->advertised, advertised);
+	ethtool_convert_legacy_u32_to_link_mode(eee->lp_advertised, lp_advertised);
 	eee->eee_enabled = port_eee_cfg.enable;
 	eee->eee_active = port_eee_cfg.eee_status;
 	eee->tx_lpi_enabled = port_eee_cfg.lpi_tx_enable;
@@ -281,12 +287,13 @@ static int32_t nss_dp_get_eee(struct net_device *netdev, struct ethtool_eee *eee
  * nss_dp_set_eee()
  *	Set EEE settings.
  */
-static int32_t nss_dp_set_eee(struct net_device *netdev, struct ethtool_eee *eee)
+static int32_t nss_dp_set_eee(struct net_device *netdev, struct ethtool_keee *eee)
 {
 	struct nss_dp_dev *dp_priv = (struct nss_dp_dev *)netdev_priv(netdev);
 	fal_port_eee_cfg_t port_eee_cfg, port_eee_cur_cfg;
 	uint32_t port_id, pos;
 	sw_error_t ret;
+	u32 advertised = 0;
 
 	memset(&port_eee_cfg, 0, sizeof(fal_port_eee_cfg_t));
 	memset(&port_eee_cur_cfg, 0, sizeof(fal_port_eee_cfg_t));
@@ -306,8 +313,10 @@ static int32_t nss_dp_set_eee(struct net_device *netdev, struct ethtool_eee *eee
 	/*
 	 * Translate the ethtool speed types to FAL speed types.
 	 */
-	while (eee->advertised) {
-		pos = ffs(eee->advertised);
+	if (!ethtool_convert_link_mode_to_legacy_u32(&advertised, eee->advertised))
+		return -EIO;
+	while (advertised) {
+		pos = ffs(advertised);
 		switch (1 << (pos - 1)) {
 		case ADVERTISED_10baseT_Full:
 			if (port_eee_cur_cfg.capability & FAL_PHY_EEE_10BASE_T) {
@@ -359,7 +368,7 @@ static int32_t nss_dp_set_eee(struct net_device *netdev, struct ethtool_eee *eee
 			return -EIO;
 		}
 
-		eee->advertised &= (~(1 << (pos - 1)));
+		advertised &= (~(1 << (pos - 1)));
 	}
 
 	port_eee_cfg.lpi_tx_enable = eee->tx_lpi_enabled;
diff --git a/nss_dp_main.c b/nss_dp_main.c
index dbc9850..40b2fb6 100644
--- a/nss_dp_main.c
+++ b/nss_dp_main.c
@@ -17,6 +17,7 @@
  */
 
 #include <linux/kernel.h>
+#include <linux/err.h>
 #include <linux/module.h>
 #include <linux/version.h>
 #include <linux/of.h>
@@ -38,10 +39,16 @@
 #if (LINUX_VERSION_CODE > KERNEL_VERSION(6, 6, 0))
 #include <net/netdev_rx_queue.h>
 #endif
+#ifdef CONFIG_RFS_ACCEL
+#include <linux/hash.h>
+#include <net/hotdata.h>
+#include <net/rps.h>
+#endif
 #if defined(NSS_DP_MAC_POLL_SUPPORT)
 #include <init/ssdk_init.h>
 #endif
 #include "nss_dp_hal.h"
+#include "nss_dp_ppeds.h"
 
 #define JUMBO_MRU_3K 3072
 
@@ -480,6 +487,9 @@ static int nss_dp_open(struct net_device *netdev)
 		netdev_dbg(netdev, "Data plane open failed\n");
 		return -EAGAIN;
 	}
+	netdev_info(netdev, "nss-dp open macid %u phydev %s phy_node %pOF\n",
+			dp_priv->macid, dp_priv->phydev ? "yes" : "no",
+			dp_priv->phy_node);
 
 	/*
 	 * Notify GMAC to start receive/transmit
@@ -493,8 +503,9 @@ static int nss_dp_open(struct net_device *netdev)
 
 	netif_start_queue(netdev);
 
-	if (!dp_priv->link_poll) {
+	if (!dp_priv->phydev) {
 		/* Notify data plane link is up */
+		netdev_info(netdev, "nss-dp no phydev, forcing data plane link up\n");
 		if (dp_priv->data_plane_ops->link_state(dp_priv->dpc, 1)) {
 			netdev_dbg(netdev, "Data plane set link failed\n");
 			return -EAGAIN;
@@ -550,17 +561,17 @@ static int nss_dp_rx_flow_steer(struct net_device *netdev, const struct sk_buff
 	}
 
 	hash = skb_get_hash(skb);
-	rxflow = &flow_table->flows[hash & flow_table->mask];
+	rxflow = &flow_table->flows[hash_32(hash, flow_table->log)];
 	rxcpu = (uint32_t)rxflow->cpu;
 
-	sock_flow_table = rcu_dereference(rps_sock_flow_table);
+	sock_flow_table = rcu_dereference(net_hotdata.rps_sock_flow_table);
 	if (!sock_flow_table) {
 		netdev_dbg(netdev, "Global RPS flow table not found\n");
 		return -EINVAL;
 	}
 
 	rfscpu = sock_flow_table->ents[hash & sock_flow_table->mask];
-	rfscpu &= rps_cpu_mask;
+	rfscpu &= net_hotdata.rps_cpu_mask;
 
 	if (rxcpu == rfscpu)
 		return 0;
@@ -723,6 +734,23 @@ static int32_t nss_dp_of_get_pdata(struct device_node *np,
 	hal_pdata->netdev = netdev;
 	hal_pdata->macid = dp_priv->macid;
 
+	dp_priv->phy_node = of_parse_phandle(np, "phy-handle", 0);
+	if (!dp_priv->phy_node) {
+		if (of_phy_is_fixed_link(np)) {
+			int ret = of_phy_register_fixed_link(np);
+
+			if (ret < 0) {
+				pr_err("%s: fail to register fixed-link: %d\n",
+						np->name, ret);
+				return -EFAULT;
+			}
+		}
+		dp_priv->phy_node = of_node_get(np);
+	}
+
+	if (!dp_priv->phy_node)
+		pr_err("%s: no phy-handle or fixed-link found\n", np->name);
+
 #if (LINUX_VERSION_CODE < KERNEL_VERSION(6, 1, 0))
 	dp_priv->phy_mii_type = of_get_phy_mode(np);
 #else
@@ -730,18 +758,6 @@ static int32_t nss_dp_of_get_pdata(struct device_node *np,
 		return -EFAULT;
 #endif
 
-	dp_priv->link_poll = of_property_read_bool(np, "qcom,link-poll");
-	if (of_property_read_u32(np, "qcom,phy-mdio-addr",
-		&dp_priv->phy_mdio_addr) && dp_priv->link_poll) {
-#ifdef NSS_DP_EDMA_I2C_BUS_ENABLE
-		dp_priv->phy_mdio_addr = FAL_SFP_PHY_ADDR;
-#else
-		pr_err("%s: mdio addr required if link polling is enabled\n",
-				np->name);
-		return -EFAULT;
-#endif
-	}
-
 	of_property_read_u32(np, "qcom,forced-speed", &dp_priv->forced_speed);
 	of_property_read_u32(np, "qcom,forced-duplex", &dp_priv->forced_duplex);
 
@@ -834,69 +850,6 @@ static int32_t nss_dp_of_get_pdata(struct device_node *np,
 	return 0;
 }
 
-/*
- * nss_dp_mdio_attach()
- */
-static struct mii_bus *nss_dp_mdio_attach(struct platform_device *pdev)
-{
-	struct device_node *mdio_node;
-#ifdef NSS_DP_EDMA_I2C_BUS_ENABLE
-	struct device_node *i2c_node;
-#endif
-	struct platform_device *mdio_plat;
-#if (LINUX_VERSION_CODE < KERNEL_VERSION(6,1,0))
-	struct ipq40xx_mdio_data *mdio_data;
-#endif
-
-#ifdef NSS_DP_EDMA_I2C_BUS_ENABLE
-	/*
-	 * Find mii_bus using "i2c-bus" handle.
-	 */
-	i2c_node = of_parse_phandle(pdev->dev.of_node, "i2c-bus", 0);
-	if (i2c_node) {
-		return of_mdio_find_bus(i2c_node);
-	}
-#endif
-
-	/*
-	 * Find mii_bus using "mdio-bus" handle.
-	 */
-	mdio_node = of_parse_phandle(pdev->dev.of_node, "mdio-bus", 0);
-	if (mdio_node) {
-		return of_mdio_find_bus(mdio_node);
-	}
-
-	mdio_node = of_find_compatible_node(NULL, NULL, "qcom,qca-mdio");
-	if (!mdio_node) {
-		mdio_node = of_find_compatible_node(NULL, NULL,
-							"qcom,ipq40xx-mdio");
-		if (!mdio_node) {
-			dev_err(&pdev->dev, "cannot find mdio node by phandle\n");
-			return NULL;
-		}
-	}
-
-	mdio_plat = of_find_device_by_node(mdio_node);
-	if (!mdio_plat) {
-		dev_err(&pdev->dev, "cannot find platform device from mdio node\n");
-		of_node_put(mdio_node);
-		return NULL;
-	}
-
-#if (LINUX_VERSION_CODE >= KERNEL_VERSION(6,1,0))
-	return dev_get_drvdata(&mdio_plat->dev);
-#else
-	mdio_data = dev_get_drvdata(&mdio_plat->dev);
-	if (!mdio_data) {
-		dev_err(&pdev->dev, "cannot get mii bus reference from device data\n");
-		of_node_put(mdio_node);
-		return NULL;
-	}
-
-	return mdio_data->mii_bus;
-#endif
-}
-
 #ifdef CONFIG_NET_SWITCHDEV
 /*
  * nss_dp_is_phy_dev()
@@ -911,7 +864,7 @@ bool nss_dp_is_phy_dev(struct net_device *dev)
 /*
  * nss_dp_adjust_link()
  */
-void nss_dp_adjust_link(struct net_device *netdev)
+static void nss_dp_adjust_link(struct net_device *netdev)
 {
 	struct nss_dp_dev *dp_priv = netdev_priv(netdev);
 	int current_state = dp_priv->link_state;
@@ -926,8 +879,9 @@ void nss_dp_adjust_link(struct net_device *netdev)
 		return;
 
 	if (current_state == __NSS_DP_LINK_DOWN) {
-		netdev_info(netdev, "PHY Link up speed: %d\n",
-						dp_priv->phydev->speed);
+		netdev_info(netdev, "PHY Link up speed: %d duplex: %d phy: %s\n",
+				dp_priv->phydev->speed, dp_priv->phydev->duplex,
+				phydev_name(dp_priv->phydev));
 		if (dp_priv->data_plane_ops->link_state(dp_priv->dpc, 1)) {
 			netdev_dbg(netdev, "Data plane set link up failed\n");
 			return;
@@ -945,6 +899,24 @@ void nss_dp_adjust_link(struct net_device *netdev)
 	}
 }
 
+static void nss_dp_phy_disconnect(struct nss_dp_dev *dp_priv)
+{
+	struct device_node *np = dp_priv->pdev->dev.of_node;
+
+	if (dp_priv->phydev) {
+		phy_disconnect(dp_priv->phydev);
+		dp_priv->phydev = NULL;
+
+		if (of_phy_is_fixed_link(np))
+			of_phy_deregister_fixed_link(np);
+	}
+
+	if (dp_priv->phy_node) {
+		of_node_put(dp_priv->phy_node);
+		dp_priv->phy_node = NULL;
+	}
+}
+
 /*
  * nss_dp_probe()
  */
@@ -954,19 +926,27 @@ static int32_t nss_dp_probe(struct platform_device *pdev)
 	struct nss_dp_dev *dp_priv;
 	struct device_node *np = pdev->dev.of_node;
 	struct nss_gmac_hal_platform_data gmac_hal_pdata;
+	const char *name = of_get_property(np, "label", NULL);
 	int32_t ret = 0;
-	uint8_t phy_id[MII_BUS_ID_SIZE + 3];
+	int assign_type;
 #if defined(NSS_DP_PPE_SUPPORT)
 	uint32_t vsi_id;
 	fal_port_t port_id;
 #endif
 
+	if (name) {
+		assign_type = NET_NAME_PREDICTABLE;
+	} else {
+		name = "eth%d";
+		assign_type = NET_NAME_ENUM;
+	}
+
 	/* TODO: See if we need to do some SoC level common init */
 
-	netdev = alloc_etherdev_mqs(sizeof(struct nss_dp_dev),
-			NSS_DP_NETDEV_TX_QUEUE_NUM, NSS_DP_NETDEV_RX_QUEUE_NUM);
+	netdev = alloc_netdev_mqs(sizeof(struct nss_dp_dev), name, assign_type,
+			ether_setup, NSS_DP_NETDEV_TX_QUEUE_NUM, NSS_DP_NETDEV_RX_QUEUE_NUM);
 	if (!netdev) {
-		pr_info("alloc_etherdev() failed\n");
+		dev_err(&pdev->dev, "alloc_netdev_mqs() failed\n");
 		return -ENOMEM;
 	}
 
@@ -1036,22 +1016,18 @@ static int32_t nss_dp_probe(struct platform_device *pdev)
 
 	dp_priv->drv_flags |= NSS_DP_PRIV_FLAG(INIT_DONE);
 
-	if (dp_priv->link_poll) {
-		dp_priv->miibus = nss_dp_mdio_attach(pdev);
-		if (!dp_priv->miibus) {
-			netdev_dbg(netdev, "failed to find miibus\n");
-			goto phy_setup_fail;
-		}
-		snprintf(phy_id, MII_BUS_ID_SIZE + 3, PHY_ID_FMT,
-				dp_priv->miibus->id, dp_priv->phy_mdio_addr);
-
-		dp_priv->phydev = phy_connect(netdev, phy_id,
-				&nss_dp_adjust_link,
-				dp_priv->phy_mii_type);
-		if (IS_ERR(dp_priv->phydev)) {
-			netdev_dbg(netdev, "failed to connect to phy device\n");
+	if (dp_priv->phy_node) {
+		dp_priv->phydev = of_phy_connect(netdev, dp_priv->phy_node,
+				&nss_dp_adjust_link, 0, dp_priv->phy_mii_type);
+		if (!dp_priv->phydev) {
+			netdev_err(netdev, "failed to connect to phy device\n");
 			goto phy_setup_fail;
 		}
+
+		netdev_info(netdev, "nss-dp connected phy %s node %pOF mode %u macid %u\n",
+				phydev_name(dp_priv->phydev), dp_priv->phy_node,
+				dp_priv->phy_mii_type, dp_priv->macid);
+		phy_attached_info(dp_priv->phydev);
 	}
 
 #if defined(NSS_DP_PPE_SUPPORT)
@@ -1115,6 +1091,7 @@ static int32_t nss_dp_probe(struct platform_device *pdev)
 	return 0;
 
 phy_setup_fail:
+	nss_dp_phy_disconnect(dp_priv);
 	dp_priv->data_plane_ops->deinit(dp_priv->dpc);
 data_plane_init_fail:
 	dp_priv->gmac_hal_ops->exit(dp_priv->gmac_hal_ctx);
@@ -1130,7 +1107,7 @@ fail:
  * Note: We only remove the physical ports here. Virtual
  * port devices are removed explicitly by the VP module.
  */
-static int nss_dp_remove(struct platform_device *pdev)
+static void nss_dp_remove(struct platform_device *pdev)
 {
 	uint32_t i;
 	struct nss_dp_dev *dp_priv;
@@ -1145,10 +1122,7 @@ static int nss_dp_remove(struct platform_device *pdev)
 		dp_ops = dp_priv->data_plane_ops;
 		hal_ops = dp_priv->gmac_hal_ops;
 
-		if (dp_priv->phydev) {
-			phy_disconnect(dp_priv->phydev);
-			dp_priv->phydev = NULL;
-		}
+		nss_dp_phy_disconnect(dp_priv);
 
 #if defined(NSS_DP_PPE_SUPPORT)
 		/*
@@ -1176,8 +1150,6 @@ static int nss_dp_remove(struct platform_device *pdev)
 		free_netdev(dp_priv->netdev);
 		dp_global_ctx.nss_dp[i] = NULL;
 	}
-
-	return 0;
 }
 
 static struct of_device_id nss_dp_dt_ids[] = {
@@ -1260,7 +1232,7 @@ EXPORT_SYMBOL(nss_dp_nsm_sawf_sc_stats_read);
 /*
  * nss_dp_init()
  */
-int __init nss_dp_init(void)
+static int __init nss_dp_init(void)
 {
 	int ret, i;
 
@@ -1348,7 +1320,7 @@ int __init nss_dp_init(void)
 /*
  * nss_dp_exit()
  */
-void __exit nss_dp_exit(void)
+static void __exit nss_dp_exit(void)
 {
 	/*
 	 * Ensure netdev remove is done before HAL cleanup.
@@ -1366,3 +1338,4 @@ module_exit(nss_dp_exit);
 
 MODULE_LICENSE("Dual BSD/GPL");
 MODULE_DESCRIPTION("NSS Data Plane Network Driver");
+MODULE_INFO(description, "NSS Data Plane Network Driver");
